# For local development, keep this commented to use Vite proxy (/api -> localhost:8000).
# VITE_API_BASE_URL=/api/v1

# For production, set your deployed backend API base, for example:
# VITE_API_BASE_URL=https://your-domain.com/api/v1
